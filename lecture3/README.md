# jsDSA

git status                    # Check changes in jsDSA files (e.g., index.html, lecture3.js)
git add .                     # Stage all changes in the jsDSA project
git add index.html            # Stage only the HTML file
git commit -m "Updated lecture3.js"  # Commit changes with a message
git push                      # Push jsDSA updates to GitHub (origin main)
git pull                      # Pull latest jsDSA changes from GitHub
git log --oneline             # View commit history for jsDSA
git checkout -b feature-branch  # Create a new branch for jsDSA features
git checkout main             # Switch back to main branch in jsDSA
git reset                     # Unstage files in jsDSA
git checkout -- lecture3.js   # Discard changes to lecture3.js
git remote -v                 # Confirm jsDSA remote (GitHub URL)
git clone https://github.com/JunaidKhanNiazii/jsDSA.git  # Clone jsDSA elsewhere